enum TypeCell {
  WALL,
  EMPTY,
  PACGOMME,
  SUPER_PACGOMME,
  GHOST_DOOR,
  PACMAN
}

enum Direction {
  UP,
  DOWN,
  LEFT,
  RIGHT,
  NONE
}

enum GameState {
  MENU,
  PLAYING,
  PAUSED,
  GAME_OVER
}
